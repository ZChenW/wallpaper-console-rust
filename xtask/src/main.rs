//! xtask — parity checks against the Bash reference implementation.
//! Phase 9: run with temp XDG_CONFIG_HOME, compare outputs.

fn main() {
    println!("xtask: parity checker for wallpaper-console-rust");
    println!("Usage: cargo xtask");
    println!("(parity tests not yet implemented — see docs/COMMAND_PARITY.md)");
}
